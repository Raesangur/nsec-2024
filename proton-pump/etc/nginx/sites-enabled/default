server {
	listen [::]:80 default_server;

	server_name _;

	location /api/flag {
        allow ::1;
        deny all;

	    include uwsgi_params;
	    uwsgi_pass unix:/tmp/protonpump.sock;
	}
		
	location / {
	    include uwsgi_params;
	    uwsgi_pass unix:/tmp/protonpump.sock;
	}
}
